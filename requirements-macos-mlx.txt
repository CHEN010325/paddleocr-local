# Keep the MLX runtime stack locked together. mlx-vlm 0.6.3 allowed pip to
# resolve incompatible Transformers 5.x releases, causing mlx_vlm.server to
# crash during import while the one-click launcher only reported a timeout.
mlx==0.32.0
mlx-lm==0.31.3
mlx-vlm==0.6.5
transformers==5.14.1
